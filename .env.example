# Copy to .env at the repository root if you prefer a single file.
# Phase 1 uses package-level env files instead:
#   client/.env
#   server/.env
#
# See client/.env.example and server/.env.example.
